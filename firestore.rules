rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only Cloud Functions (admin SDK) can read/write offerings
    // Direct client access is blocked — all requests must go through the API
    match /offerings/{docId} {
      allow read, write: if false;
    }
  }
}
