rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow direct browser access to offerings collection
    // Anyone can read and write — suitable for internal church use
    // If you want to restrict, add Firebase Authentication later
    match /offerings/{docId} {
      allow read, write: if true;
    }
  }
}
